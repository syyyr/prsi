{
  "name": "prsi",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "build/prsi.js",
  "scripts": {
    "clean": "rm -rf build/ node_modules/",
    "build": "npx tsc",
    "start": "npm run build && node build/main.js",
    "test": "npm run build && while node build/main.js; do :; done"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.3",
    "@types/node": "^13.9.2",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-ws": "^4.0.0"
  }
}
